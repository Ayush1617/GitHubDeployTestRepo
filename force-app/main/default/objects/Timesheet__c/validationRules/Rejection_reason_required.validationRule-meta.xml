<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Rejection_reason_required</fullName>
    <active>true</active>
    <description>If Status has Rejected than Rejection Reason must be filled</description>
    <errorConditionFormula>AND(
   ISCHANGED(Status__c),
   ISPICKVAL(Status__c , &quot;Rejected&quot;),
   ISBLANK( Rejection_Reason__c )
)</errorConditionFormula>
    <errorMessage>Please provide a rejection reason before rejecting the timesheet.</errorMessage>
</ValidationRule>
