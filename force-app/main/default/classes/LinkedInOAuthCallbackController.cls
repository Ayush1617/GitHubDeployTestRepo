public with sharing class LinkedInOAuthCallbackController {
    
    // This controller is intentionally minimal - all processing happens via JavaScript postMessage
    public LinkedInOAuthCallbackController() {
        // No server-side processing needed
    }
}